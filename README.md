**currently in development, do not expect everything to work**

start server with
`mvn -Drun.profiles=dev spring-boot:run`

when using the "dev" profile, the following users are available (password equals username):
* test
* testadmin
